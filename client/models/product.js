class Product {
    constructor({id, title, datetime, parameter, author, icons}) {
        this.id = id;
        this.title = title;
        this.datetime = datetime;
        this.parameter = parameter;
        this.author = author;
        this.icons = icons;
    }
}

export default Product;
